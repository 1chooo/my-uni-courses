# HW02

``` c
// UP(N), RIGHT(E), DOWN(S), LEFT(W) (find the way through clockwise)
// {row, col}

/*
nextRow = row + move[dir].vert;
nextCol = col + move[dir].horiz;
*/

// main we at least need m+2  by n+2 array

// for (int i = 0; i < row + 2; i++) {
  //   for (int j = 0; j < col + 2; j++) {
  //   printf("%d ", maze[i][j]);
  //   }
  //   printf("\n");
  // }
```
